{
  "name": "MyPortfolio",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://shivs077.github.io",
  "dependencies": {
    "gh-pages": "^3.2.3",
    "node-sass": "^7.0.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^5.1.2",
    "react-scripts": "4.0.3",
    "react-transition-group": "^4.4.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "sass": "node-sass -w scss/ -o src/css/ --recursive",
    "predeploy": "rm -rf build && yarn build",
    "deploy": "yarn predeploy && gh-pages -b master -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^3.1.1",
    "prettier": "^2.3.2"
  }
}
